# Status

`Status` is a small coarse-grained enum representing whether an operation succeeded or failed.

## Values

- `Failed`
- `Successful`

Use it when a simple outcome signal is enough and a full `QueryResult` is unnecessary.
