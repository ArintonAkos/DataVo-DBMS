# DataTypes

`DataTypes` defines the primitive catalog types currently supported by the engine.

## Supported values

- `Int`
- `Float`
- `Bit`
- `Date`
- `Varchar`

## Where it is used

- catalog metadata (`Field`)
- schema evolution (`ALTER TABLE`)
- storage serialization decisions
