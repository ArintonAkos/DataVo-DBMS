# Types (Parser) Overview
The `Types` module encapsulates specific run-time data structures required strictly for managing volatile in-memory states mapped physically during complex procedural operations generated inside the parser. These types exist ephemeral tracking array buffers executing logical filtering combinations safely identifying cross-joined constraints aggressively.

## Core Responsibilities
* **Volatile Structuring:** Represents transient schemas dynamically resolving arrays built physically via the integration bridging table namespaces.
* **Array Indexing:** Establishes strong identification parameters resolving duplicate row keys aggressively sorting relational datasets tracking parent overlaps continuously targeting precise internal buffers natively.

## Component Breakdown

| Component (File) | Architectural Role |
|------------------|--------------------|
| `GroupedTable.cs` | Contains specialized collection groupings wrapping filtered metrics representing intermediate chunks mapped dynamically capturing scalar targets recursively mapped against identical keys linearly. |
| `HashedTable.cs` | Represents high-speed array indexes resolving equality mappings mapping nested boundaries securely accelerating join processing loops natively tracking indexed overlaps efficiently without tree traversals actively bridging keys. |
| `JoinedRow.cs, JoinedRowId.cs` | Represents transient row abstractions tracking array values merging records wrapping explicit alias names mitigating table collisions cleanly ensuring unique keys internally resolving dynamically matched values exactly. |
| `ListedTable.cs` | Ephemeral array buffer capturing sorted records continuously. |
| `Row.cs` | Extends physical generic schemas defining internal row interfaces wrapping memory mapped outputs standardizing operations against abstract targets uniformly validating states linearly tracking values cleanly across expressions smoothly integrating evaluations universally. |

## Dependencies & Interactions
These unique structural components never write to the disk persistently evaluating exclusively via `Mechanism` loop processes integrating `JoinStrategies` orchestrators processing transient data manipulating queries capturing variables flawlessly across logical paths cleanly resolving outputs accurately parsing trees fluently returning outcomes effortlessly.
