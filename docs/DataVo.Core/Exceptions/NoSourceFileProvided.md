# NoSourceFileProvided

`NoSourceFileProvided` signals that a file-based operation was invoked without a required source path.

Use this exception when the calling contract requires a file input and the absence of that file is a programming/configuration error.
