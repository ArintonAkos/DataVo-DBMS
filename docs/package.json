{
  "name": "docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docs:ai": "node scripts/generate-ai-docs.cjs",
    "docs:dev": "npm run docs:ai && vitepress dev",
    "docs:build": "npm run docs:ai && node scripts/clean-docs-dist.cjs && vitepress build",
    "benchmarks:linux-plots": "python3 scripts/generate-linux-benchmark-plots.py",
    "mcp": "node mcp/datavo-docs-server.cjs",
    "test:browser:generate": "bash ../scripts/generate-wasm-browser-scenarios.sh",
    "test:browser:install": "playwright install chromium",
    "test:browser": "npm run test:browser:generate && playwright test",
    "test:browser:needs-specific": "npm run test:browser:generate && rm -f tests/browser/generated/wasm-needs-specific.execution-summary*.json tests/browser/generated/wasm-parity.overall-summary.json && playwright test tests/browser/datavo-wasm-needs-specific-*.spec.ts && playwright test tests/browser/datavo-wasm-needs-summary.spec.ts",
    "test:browser:strict": "npm run test:browser:generate && DATAVO_STRICT_BROWSER_PARITY=1 playwright test",
    "test:browser:strict:stress": "DATAVO_STRICT_BROWSER_PARITY=1 playwright test -g \"strict parity remains stable across repeated loops|strict parity backend matrix\"",
    "test:browser:summary": "node ../scripts/browser-parity-summary.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@codemirror/commands": "^6.9.1",
    "@codemirror/lang-sql": "^6.10.0",
    "@codemirror/language": "^6.11.3",
    "@codemirror/state": "^6.5.2",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@codemirror/view": "^6.38.2",
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0",
    "codemirror": "^6.0.2",
    "esbuild": "^0.27.3",
    "mermaid": "^11.12.3",
    "vitepress": "^1.6.4",
    "vitepress-plugin-mermaid": "^2.0.17",
    "vue": "^3.5.29"
  },
  "overrides": {
    "esbuild": "$esbuild"
  }
}
